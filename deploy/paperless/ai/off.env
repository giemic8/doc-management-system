# No LLM at all. OCR, full-text search, tags, workflows and the whole
# archive work unchanged; only the metadata suggestions are gone.
#
# Useful as the answer to "is the AI causing this?" -- switch, restart,
# reproduce.

PAPERLESS_AI_ENABLED=false
